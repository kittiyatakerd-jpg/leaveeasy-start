# Backlog — LeaveEasy

รายการงานที่ตั้งใจเลื่อนออกไปทำทีหลัง พร้อมเหตุผลว่าทำไมยังไม่ทำตอนนี้

## 🔴 ย้ายคีย์ AI ไปฝั่งที่ผู้ใช้แตะไม่ได้

**พบเมื่อ:** สัปดาห์ที่ 8 ขั้น A2 — เปิดดูไฟล์ [js/ai-config.js](js/ai-config.js) ตรงๆ ผ่านเบราว์เซอร์
(เช่น `http://localhost:5173/js/ai-config.js` หรือ URL จริงบน Firebase Hosting) เห็นคีย์ OpenRouter
เต็มตัวในไฟล์ทันที ไม่ต้องเปิด DevTools ขั้นสูงด้วยซ้ำ — เพราะไฟล์นี้เป็นไฟล์ static ที่เบราว์เซอร์ต้องโหลด
มาทำงานฝั่ง client เท่านั้น จึงไม่มีทางซ่อนได้ตราบใดที่ยังเรียก OpenRouter ตรงจากเบราว์เซอร์

**สถานะตอนนี้:** `js/ai-config.js` อยู่ใน `.gitignore` แล้ว ดังนั้น**คีย์ไม่หลุดขึ้น GitHub** ก็จริง
แต่คีย์ยังหลุดออกจากเบราว์เซอร์ของทุกคนที่เปิดเว็บอยู่ดี (เห็นได้จาก Network tab หรือเปิดไฟล์ตรงๆ)
เป็นคนละปัญหากับ "หลุดขึ้น git" — ยังไม่ได้แก้จุดนี้

**ทางแก้ที่ถูกต้อง (ยังไม่ทำ):** ย้ายการเรียก OpenRouter ไปทำฝั่งเซิร์ฟเวอร์แทน เช่น
[Cloud Functions for Firebase](https://firebase.google.com/docs/functions) — เบราว์เซอร์เรียกฟังก์ชันของเราเอง
(ที่ต้องล็อกอินก่อนถึงเรียกได้) แล้วฟังก์ชันนั้นถือคีย์ไว้ฝั่งเซิร์ฟเวอร์ค่อยไปเรียก OpenRouter ต่ออีกที
คีย์จะไม่ถูกส่งมาถึงเบราว์เซอร์ของผู้ใช้เลยแม้แต่ครั้งเดียว

**ทำไมยังไม่ทำตอนนี้:** อยู่นอกขอบเขตของ Module 2 (ใช้ Firebase Functions ซึ่งเรียน Module อื่น)
และใบงานสัปดาห์ที่ 8 ยอมรับให้เรียกจากฝั่งเบราว์เซอร์ไปก่อนโดยรู้ข้อจำกัดนี้ชัดเจน
