# ACL — สิทธิ์การใช้งานตามบทบาท (LeaveEasy)

อ้างอิงจาก [leaveeasy-spec.md](leaveeasy-spec.md) หัวข้อ 2 — ค่าที่เก็บในช่อง `role` ของโฟลเดอร์ `users`
ต้องเป็นรหัสภาษาอังกฤษเสมอ (`employee` / `manager` / `hr`)

| บทบาท | `role` | ทำอะไรได้ | ทำอะไรไม่ได้ |
|---|---|---|---|
| 🙋 ผู้ขอลา | `employee` | ยื่นใบขอลาใหม่ · ดูใบลาของตัวเอง · เขียนความเห็นเพิ่มในใบของตัวเอง · ลบใบของตัวเองที่สถานะยังเป็น `รอพิจารณา` | เปลี่ยนสถานะใบลา · ดูใบลาของคนอื่น · แก้ประเภทการลา |
| ✅ ผู้อนุมัติ | `manager` | ดูใบลาทุกใบ · เปลี่ยนสถานะใบลา (อนุมัติ/ไม่อนุมัติ) · เขียนความเห็นในทุกใบ | ลบใบลาของคนอื่น · แก้ประเภทการลา |
| 🧾 ฝ่ายบุคคล | `hr` | ทำได้ทุกอย่างที่ผู้อนุมัติทำได้ · กำหนดผู้อนุมัติให้แต่ละใบ · เพิ่ม/แก้/ลบประเภทการลา · เห็นแดชบอร์ดสรุปทั้งระบบ | — |

สมัครสมาชิกใหม่ทุกคนได้ `role: employee` เป็นค่าเริ่มต้นเสมอ (ดู [js/login.js](js/login.js)) —
การเลื่อนเป็น `manager`/`hr` ทำโดยแก้ไฟล์ผู้ใช้นั้นตรงในโฟลเดอร์ `users` ผ่าน Firebase Console เท่านั้น
ยังไม่มีหน้าจอจัดการสิทธิ์ในระบบ

## สถานะการบังคับใช้จริงในโค้ด

| สัปดาห์ | มีล็อกอินไหม | แยกสิทธิ์ตาม role ไหม |
|---|---|---|
| 6 | ไม่มี — ทุกหน้าเปิดใช้ได้หมด | ไม่มี |
| **7 (ตอนนี้)** | **มี — ต้องล็อกอินก่อนถึงเข้าหน้าไหนก็ได้** (`js/auth-guard.js`) และ Firestore Rules บังคับ `request.auth != null` กับทุกโฟลเดอร์รวม `approvals` | **ยังไม่มี** — ล็อกอินแล้วทำได้ทุกปุ่มเหมือนกันหมด ไม่ว่า role ไหน (ตามที่ตั้งใจไว้ในสเปก §2: "ข้อจำกัดรายบทบาทในตารางนี้เริ่มมีผลจริงในสัปดาห์ที่ 8") |
| 8 | เหมือนเดิม | จะเพิ่ม Security Rules ที่เช็ค `role` และ `requesterId == request.auth.uid` จริง พร้อมซ่อน/แสดงปุ่มตามบทบาทในหน้าจอ |

⚠️ **ข้อจำกัดที่ต้องรู้ตอนนี้:** เพราะยังไม่แยกสิทธิ์ตาม role พนักงานคนหนึ่งที่ล็อกอินอยู่ยังเปิดดู แก้สถานะ
และลบใบลาของคนอื่นได้อยู่ (ตราบใดที่ยังเป็นสถานะ `รอพิจารณา`) — เป็นข้อจำกัดที่ยอมรับได้สำหรับสัปดาห์นี้
เพราะจุดประสงค์หลักคือกันคนที่ **ไม่ได้ล็อกอินเลย** ออกจากระบบก่อน
